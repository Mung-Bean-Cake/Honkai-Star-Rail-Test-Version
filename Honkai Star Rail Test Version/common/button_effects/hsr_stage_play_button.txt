#话剧
rewrite_plays_effect = {
   allow = {
        custom_tooltip = {
            fail_text = need_rewrite_plays_effect_tt
            hidden_trigger = {
                from = {
                    has_country_flag = allow_show_time
                }
            }
        }
    }
    potential = {
        always = yes
    }
    effect = {
        custom_tooltip = can_select
        hidden_effect = {
            country_event = { id = xiaoqiaosi.4000 }
        }
    }
}
hsr_star_show_effect = {
    allow = {
        always = yes
    }
    potential = {
        always = yes
    }
    effect = {
        custom_tooltip = hsr_star_show_effect
    }
}
hsr_star_play_1_start_effect = {
    allow = {
         custom_tooltip = {
            fail_text = has_choose
            hidden_trigger = {
                from = {
                    not = {has_country_flag = hsr_star_play_1_choose}
                    has_country_flag = hsr_star_play_0_set
                }
            }
        }
        
    }
    potential = {
        always = yes
    }
    effect = {
        set_country_flag = hsr_star_play_1_choose
        remove_country_flag = hsr_star_play_0_set
        set_variable = {
            which = hsr_star_play_0
            value = 1
        }
    }
}



