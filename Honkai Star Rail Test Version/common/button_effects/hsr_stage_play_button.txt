#话剧
rewrite_plays_effect = {
   allow = {
        custom_tooltip = {
            fail_text = need_rewrite_plays_effect_tt
            hidden_trigger = {
                from = {
                    has_country_flag = allow_show_time
                }
            }
        }
    }
    potential = {
        always = yes
    }
    effect = {
        custom_tooltip = can_select
        hidden_effect = {
            country_event = { id = xiaoqiaosi.4000 }
        }
    }
}
hsr_star_show_effect = {
    allow = {
        always = yes
    }
    potential = {
        always = yes
    }
    effect = {
        custom_tooltip = hsr_star_show_effect
    }
}
hsr_star_play_1_use_effect = {
    allow = {
        custom_tooltip = {
            fail_text = need_tech_path_civic_1_tt
            hidden_trigger = {
               
            }
        }
    }
    potential = {
        always = yes
    }
    effect = {
        custom_tooltip = can_select
        hidden_effect = {
            country_event = { id = xiaoqiaosi.4000 }
        }
    }
}

inline_script = {
    script = button_effect/hsr_stage_play
    civic_name = tr_zhishi_jiyi_2
    value = 1
}

