#飞升：为了守护和捍卫
#选择留在母星并完成主线后解锁
ap_jarilo_saved = {
	potential = {
		not = { has_ascension_perk = ap_jarilo_saved }
		has_country_flag = SRB_main_story_finished
	}

	possible = {
		custom_tooltip = {
			fail_text = "ap_jarilo_saved_fail"
		}
	}
}
#!缺一个图标
